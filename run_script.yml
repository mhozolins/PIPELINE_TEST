name: HELLO WORLD

on:
  push:
    branches:
      - main

jobs:
  run:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Run Python
        run: python hello_world.py